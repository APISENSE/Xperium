var CarbonFootprint = (function() {
	'use strict';

	var CarbonFootprint = {
			
			/**
			 * Compute the carbon footprint of a ride based on the type
			 * and the duration of the given ride.
			 * 
			 * @param ride
			 */
			computeCarbonFootprint: function(ride) {
				return;
			}
	};

	return CarbonFootprint;

}());

module.exports = CarbonFootprint;